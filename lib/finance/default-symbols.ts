/**
 * Default list of symbols displayed on the dashboard when the user has no
 * prior activity. Exposed as a shared constant so both server and client
 * components can reference the same symbols without crossing the React
 * Server Components boundary.
 */
export const DEFAULT_SYMBOLS = ['AAPL', 'MSFT', 'BTC-USD'];

export default DEFAULT_SYMBOLS;
